name: Shared

on:
  workflow_call:

permissions:
  contents: read

jobs:
  lint:
    uses: ./.github/workflows/lint.yml

  checks:
    needs: lint
    permissions:
      contents: read
      id-token: write
    uses: ./.github/workflows/checks.yml
