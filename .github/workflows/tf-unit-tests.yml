name: Terraform Unit Tests

on:
  workflow_dispatch:
  workflow_call:

permissions: {}

jobs:
  terraform-unit-tests:
    name: Terraform Unit Tests
    permissions:
      contents: read
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
